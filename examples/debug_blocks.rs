# Debug Blocks
# Lists all supported components horizontally with 1 gap spacing

# 1. Piston
piston = Piston(position: (0, 5, 0), facing: up)

# 2. Sticky Piston
sticky = StickyPiston(position: (2, 5, 0), facing: up)

# 3. Repeater
repeater = Repeater(position: (4, 5, 0), facing: north)

# 4. Lever
lever = Lever(position: (6, 5, 0))

# 5. Lamp
lamp = Lamp(position: (8, 5, 0))

# 6. Observer
observer = Observer(position: (10, 5, 0), facing: up)

# 7. Dropper
dropper = Dropper(position: (12, 5, 0), facing: up)

# 8. Comparator
comparator = Comparator(position: (14, 5, 0), facing: north)

# 9. Hopper
hopper = Hopper(position: (16, 5, 0))

# 10. Target
target = Target(position: (18, 5, 0))

# 11. Slime Block
slime = SlimeBlock(position: (20, 5, 0))

# 12. Honey Block
honey = HoneyBlock(position: (22, 5, 0))

# 13. Redstone Torch
torch = RedstoneTorch(position: (24, 5, 0))

# 14. Pressure Plate
plate = PressurePlate(position: (26, 5, 0))

# 15. Button
button = Button(position: (28, 5, 0))
